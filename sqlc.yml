version: "2"
sql:
  - engine: "sqlite"
    queries: "sql/queries/*"
    schema: "sql/schema.sql"
    gen:
      go:
        package: "db"
        out: "db"
        emit_json_tags: true
        json_tags_case_style: "camel"

